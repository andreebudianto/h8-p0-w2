// Tugas nomor 1
var rows1 = 5;
while(rows1 > 0) {
    console.log('*');
    rows1--;
}
// Tugas nomor 2
var rows = 5;
while (rows > 0) {
  var cetak = '';
  var rows1 = 5;
    while (rows1 > 0) { 
        var cetak = cetak + '*';
        rows1 --;
    } 
    console.log (cetak);
    rows --;
} 
console.log (cetak);

// Tugas nomor 3
var rows3 = 5;
var temp = '';
while (rows3 > 0) {
    
    var temp = temp + '*';
    rows3 --;
  
  console.log(temp)
}
